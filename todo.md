TODO - Eshop (2025-12-07)

Stav: rozpracováno — níže jsou aktuální úkoly, poznámky a priorita.

1) Clean & build
- Stav: dokončeno
- Popis: Vymazat `.next` a spustit `npm run build` pro regeneraci serverových artefaktů.
- Poznámka: Potvrzeno — `page.js` existuje v `.next/server/app/admin/dashboard`.

2) Check server artifact
- Stav: dokončeno
- Popis: Ověřit přítomnost `.next/server/app/admin/dashboard/page.js`.

3) Verify page sources
- Stav: not-started
- Popis: Zkontrolovat `app/admin/dashboard/page.tsx` a `components/admin/AdminDashboardClient.tsx` pro `"use client"` a wrapper.
- Cíl: Ujistit se, že stránka je správně rozdělena na server wrapper + client komponentu.

4) Capture build log
- Stav: not-started
- Popis: Pokud se problém vrátí, vytvořit `build.log` (přesměrovat: `npm run build > build.log 2>&1`) a prohledat chyby/varování týkající se `/admin/dashboard`.

5) Run dev for repro
- Stav: not-started
- Popis: Spustit `npm run dev` a reprodukovat chybu (GET /favicon.ico nebo přístup na `/admin/dashboard`) pro získání run-time stack trace.

6) E2E admin flow (manuální)
- Stav: not-started
- Popis: Přihlásit se na `/admin/login`, otevřít `/admin/dashboard`, upravit ketubu, uložit, ověřit, že `PUT /api/admin/ketubas/[id]` vrátí 200 a soubor `data/ketubas.json` se aktualizuje.

7) Volitelné: Commit & push
- Stav: not-started
- Popis: Pokud chcete, můžeme commitnout nové soubory (`components/admin/AdminDashboardClient.tsx`, změna `app/admin/dashboard/page.tsx`) a pushnout na `origin/main`.

Poznámky:
- Pokud chcete, mohu pokračovat a spustit čistý build a/nebo dev server teď a přidat `build.log` do repozitáře pro analýzu.

---
Generated by Copilot assistant — pokud chcete formát jinak (např. `TODO.md`, `todo.json`, nebo přidat prioritu), dejte vědět.
